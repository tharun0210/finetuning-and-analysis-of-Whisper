# Josh Talks ASR — Google Colab / Kaggle T4 requirements (Q1 training)
# Run this in a Colab/Kaggle notebook cell with a T4 GPU runtime:
#
#   !pip install -r requirements-colab.txt

# Core PyTorch — Colab pre-installs this, but pin version for consistency
torch>=2.2.0
torchaudio>=2.2.0

# Transformers ecosystem
transformers>=4.41.0
datasets>=2.18.0
evaluate>=0.4.0

# LoRA + 8-bit quantization (Linux/CUDA only)
peft>=0.10.0
bitsandbytes>=0.43.0

# Language detection (FastText — Q2)
fasttext-wheel>=0.9.2

# Spell checking (Q3)
symspellpy>=6.7.7

# WER evaluation (Q4)
editdistance>=0.8.1
jiwer>=3.0.3

# Hindi NLP normalization
indic-nlp-library>=0.91

# Data / Utilities
numpy>=1.26.0
pandas>=2.2.0
requests>=2.31.0
